package net.fightingpainter.lib.items;

import net.fabricmc.fabric.api.item.v1.FabricItemSettings;

//this class should just contain a bunch of predefined fabric item settings so that when creating item with common settings, you can just use this class instead of creating a new FabricItemSettings object every time

public class ItemSettings {
    public static FabricItemSettings DEFAULT = new FabricItemSettings();

}
